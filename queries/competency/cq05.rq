PREFIX min: <https://w3id.org/min#>

# CQ05: Epistemische Historie (Property Path, KEIN transitives supersedes)
SELECT ?latest ?earliest WHERE {
  ?latest a min:Epistemicum .
  ?latest min:supersedes+ ?earliest .
}
LIMIT 25
